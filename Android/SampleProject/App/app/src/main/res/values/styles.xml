<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="demo_chat_status_icon_style" partent="chat_textview_status_icon_style">
        <item name="chat_status_ok">@drawable/send_confirm</item>
        <item name="chat_status_pending">@drawable/send_error_2</item>
        <item name="android:layout_marginLeft">2dp</item>
        <item name="android:layout_marginBottom">2dp</item>
        <item name="android:layout_height">10dp</item>
        <item name="android:layout_width">18dp</item>
        <item name="android:visibility">gone</item>
    </style>

    <style name="demo_incoming_chat_status_icon_style">
        <item name="chat_status_broken">@drawable/broken</item>
        <item name="android:layout_marginLeft">2dp</item>
        <item name="android:layout_marginBottom">2dp</item>
        <item name="android:layout_height">8dp</item>
        <item name="android:layout_width">16dp</item>
        <item name="android:visibility">gone</item>
    </style>

    <style name="DemoSubmitButton" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">#fffefe</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:layout_marginLeft">8dp</item>
    </style>

    <style name="CustomTextAppearance" parent="Base.TextAppearance.AppCompat.Large.Inverse">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

</resources>
